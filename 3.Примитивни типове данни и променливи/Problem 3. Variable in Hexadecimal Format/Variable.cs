using System;

namespace Problem_3.Variable_in_Hexadecimal_Format
{
    class Variable
    {
        static void Main(string[] args)
        {
            int hexValue = 0xFE;
            Console.WriteLine("FE in hexadecimal format is " + hexValue + " in decimal!");
        }
    }
}
